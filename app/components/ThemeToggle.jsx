'use client';

export default function ThemeToggle({ isDark, onToggle }) {
  // We're keeping this component minimal since the app is designed for dark mode
  // The toggle is hidden but preserved for future use
  return null;
}
